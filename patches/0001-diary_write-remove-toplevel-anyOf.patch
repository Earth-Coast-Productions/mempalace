--- a/mcp_server.py
+++ b/mcp_server.py
@@ -2663,12 +2663,12 @@
                 },
             },
             # agent_name is always required; 'entry' or its alias 'content' must
-            # be present (the server remaps content->entry at dispatch).
+            # be present — the server remaps content->entry at dispatch and
+            # rejects calls missing both at handler time. Top-level anyOf
+            # removed here because Anthropic's tools API rejects oneOf/allOf/anyOf
+            # at input_schema root (HTTP 400 from Messages API). See patch
+            # tracked in earthcoast/mempalace deployment repo.
             "required": ["agent_name"],
-            "anyOf": [
-                {"required": ["entry"]},
-                {"required": ["content"]},
-            ],
         },
         "handler": tool_diary_write,
     },
